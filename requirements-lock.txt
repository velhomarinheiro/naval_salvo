# Reproducibility lockfile for the LAFusion 2026 data-farming experiment
# (spec sec 11: environment pin). Exact versions used to generate the shipped
# results. Python 3.11.15. Engine: salvo_mds.ENGINE_VERSION == "1.0.0".
#
#   python3 -m pip install -r requirements-lock.txt
#
# These pin the farm/analyze/validation pipeline ONLY (numpy stack). The
# Streamlit app has its own looser requirements.txt.
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.11.0
pytest==9.1.1
